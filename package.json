{
  "name": "graduation-memories-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Adaptive Polaroid image pipeline and static 3D university memories gallery.",
  "scripts": {
    "compress": "node scripts/process-images.mjs",
    "build": "npm run compress",
    "serve": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.2.0",
    "sharp": "^0.35.4",
    "three": "0.185.1"
  },
  "engines": {
    "node": ">=20"
  }
}
